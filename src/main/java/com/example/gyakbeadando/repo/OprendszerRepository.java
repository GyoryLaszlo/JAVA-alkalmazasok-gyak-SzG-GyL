package com.example.gyakbeadando.repo;

import com.example.gyakbeadando.model.Oprendszer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OprendszerRepository extends JpaRepository<Oprendszer, Long> {}
